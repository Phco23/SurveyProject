@using SurveyProject.Models.ViewModels
@model SurveyDetailsViewModel

<div class="container mt-5">
    <div class="row mb-4">
        <div class="col-md-8 offset-md-2">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">Survey Information</h5>
                </div>
                <div class="card-body">
                    <p><strong>Title:</strong> @Model.Title</p>
                    <p><strong>Description:</strong> @Model.Description</p>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col text-center">
            <h3 class="fw-bold">Questions & Options</h3>
            <a asp-controller="Questions" asp-action="Create" asp-route-surveyId="@Model.Id" class="btn btn-success px-4">Add Question</a>
        </div>
    </div>

    @if (Model.Questions.Any())
    {
        <div class="row">
            @foreach (var question in Model.Questions)
            {
                <div class="col-md-10 offset-md-1 mb-4">
                    <div class="card shadow-sm">
                        <div class="card-header bg-secondary text-white d-flex justify-content-between align-items-center">
                            <div>
                                <strong>@question.QuestionText - @question.QuestionTypeName</strong>
                            </div>
                            <div>
                                <a asp-controller="Questions" asp-action="Edit" asp-route-id="@question.Id" class="btn btn-sm btn-warning">Edit</a>
                                <a asp-controller="Questions" asp-action="Delete" asp-route-id="@question.Id" class="btn btn-sm btn-danger">Delete</a>
                            </div>
                        </div>
                        <div class="card-body">
                            @if (question.QuestionTypeName != "Text")
                            {
                                <table class="table table-bordered">
                                    <thead class="table-light">
                                        <tr>
                                            <th>Option Text</th>
                                            <th>Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var option in question.Options)
                                        {
                                            <tr>
                                                <td>@option.OptionText</td>
                                                <td>
                                                    <a asp-controller="Questions" asp-action="EditOption" asp-route-id="@option.Id" class="btn btn-sm btn-warning">Edit</a>
                                                    <form asp-controller="Questions" asp-action="DeleteOption" asp-route-id="@option.Id" method="post" style="display:inline;">
                                                        <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                                                    </form>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                                <a asp-controller="Questions" asp-action="AddOption" asp-route-questionId="@question.Id" class="btn btn-sm btn-primary mt-2">Add Option</a>
                            }
                            else
                            {
                                <p class="text-muted">Options are not available for this question type.</p>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="row">
            <div class="col text-center">
                <p class="text-muted">No questions available for this survey.</p>
            </div>
        </div>
    }
</div>
